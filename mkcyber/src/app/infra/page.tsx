import Navbar from "@/src/components/navbar";
import { Typography } from "@mui/material";

export default function Lab() {
    return(
        <>
            <Typography sx={{position: 'absolute', right: '50%'}}>Home Lab content will be put here</Typography>
        </>
        
    );
}